import 'package:flutter/material.dart';

class BachecaPage extends StatelessWidget {
  const BachecaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
